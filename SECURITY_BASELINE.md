# BSV Market 安全基线（MVP）

## 分级说明
- P0: 必须上线前完成，否则存在直接资金/数据安全风险。
- P1: 建议首个稳定版完成，显著降低攻击成功率。
- P2: 增强项，提升长期抗攻击与运营质量。

## P0（上线阻断项）

1. 订单状态机强校验
- 要求：任何链上订单事件都必须校验前置状态，禁止跳步、重复执行。
- 验收：构造“重复退款/未发货确认收货”等非法事件，系统全部拒绝。

2. 2-of-2 共管交易模板锁定
- 要求：托管脚本模板固定、签名流程固定、SIGHASH 策略固定，不允许动态拼接危险参数。
- 验收：脚本与签名单测覆盖“锁定/释放/退款/争议冻结”四条主路径。

3. 关键流程确认数门槛
- 要求：支付锁定、发货锁定、最终结算都需达到最小确认数后才推进状态。
- 验收：0-confirm 交易不应触发终态变更。

4. 多节点索引交叉验证
- 要求：商品与订单查询至少双源比对；关键结果支持 txid + merkle proof 抽验。
- 验收：单个恶意节点返回假数据时，客户端可识别并降权。

5. 回放补扫机制
- 要求：后台管家维护 `last_scanned_height`，重启/断线后可回放补齐。
- 验收：人为断网后恢复，缺失期间事件可被补回。

6. 密钥与助记词本地加密存储
- 要求：助记词、私钥、聊天会话密钥必须加密落盘，禁止明文。
- 验收：检查本地文件与数据库，不出现明文敏感数据。

7. 输入与载荷大小限制
- 要求：商品字段、聊天消息、图片数据有严格长度上限与格式校验。
- 验收：超大/畸形 payload 不会导致崩溃、阻塞或内存暴涨。

## P1（稳定版建议）

1. 节点信誉与自动熔断
- 要求：记录节点错误率、一致性评分；低分节点自动降级/隔离。
- 验收：模拟连续错误节点，可被自动剔除并切换。

2. 抗垃圾消息策略
- 要求：对商品发布与聊天写链增加费率下限/配额控制。
- 验收：高频垃圾写入时，本地索引服务仍可用。

3. 聊天元数据最小化
- 要求：链上仅保留最小必要元信息，消息内容全密文。
- 验收：抓链数据无法还原明文，仅可见最小结构字段。

4. 本地数据库一致性保护
- 要求：SQLite 开启 WAL；关键更新使用事务，崩溃后可恢复。
- 验收：中断写入后重启，不出现半状态订单。

5. 审计日志
- 要求：资金相关动作（锁定/释放/退款/争议）都有可追溯日志。
- 验收：任一订单能从日志还原关键操作链路。

## P2（增强项）

1. Eclipse 攻击缓解
- 要求：多网络出口、节点多样化、定时主动换邻居。
- 验收：单运营商/单网段节点异常时仍可同步。

2. 信誉与反滥用系统
- 要求：按历史交易与争议行为建立信誉评分。
- 验收：高风险账户默认受限（限额/提示/延时）。

3. 异常行为检测
- 要求：识别重复撤单、异常高频争议、刷屏攻击。
- 验收：异常模式触发告警并建议用户防护动作。

4. 恢复演练机制
- 要求：支持“新设备从助记词恢复 + 全量补同步”的自动化演练。
- 验收：定期演练通过率可量化。

## 安全验收门槛（MVP）
- 必须通过：全部 P0。
- 建议通过：P1 至少 60%。
- 可延期：P2。

## 开发顺序建议
1. 先实现 P0 的状态机与资金脚本测试。
2. 再补 P0 的多源校验与回放补扫。
3. 完成后进入 P1 的节点信誉与抗垃圾。
