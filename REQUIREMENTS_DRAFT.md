# BSV Market 需求草案（讨论版）

## 0. 项目目标
- 构建去中心化商品市场（类淘宝）：
  - 每个用户本地运行客户端即可开店/下单/聊天。
  - 商品与订单状态数据尽量上链或可由链上数据重建。
  - 不依赖中心化统计服务器。
  - 在正常运行路径中禁止任何 WhatsonChain/WOC 之类中心化链查询调用；若保留兼容入口，必须显式人工启用且默认关闭。

## 1. 角色与对象
- 角色：买家、卖家（同一账号可同时是买家和卖家）。
- 核心对象：
  - 店铺（Shop）
  - 商品（Product）
  - 订单（Order）
  - 交易托管状态（Escrow State）
  - 聊天会话（Chat Session）
  - 后台管家（Background Steward，本地常驻任务模块）

## 2. 业务流程（按你给的规则）
1. 买家A浏览卖家B商品并下单。
2. A锁定金额：商品价 + 20%确认金（可配置）。
3. B看到A已锁定后，点击发货，并锁定10%退货保证金（可配置）。
4. A确认收货：
   - 商品价转给B。
   - A的20%确认金退回A。
   - B的10%保证金退回B。
5. A申请退货（并线下退货）。
6. B确认收货无误后取消交易：
   - 商品价 + A的20%确认金退回A。
   - B的10%保证金退回B。
7. A在B发货前可主动取消，资金原路退回。
8. 争议终局（无法达成一致）：
   - A损失商品价 + 20%确认金。
   - B损失商品 + 10%保证金。

## 3. MVP 功能需求

> MVP（Minimum Viable Product）= 最小可用版本。  
> 目标是先做出“可真实跑通完整交易闭环”的第一版，而不是一次做完所有高级功能。

### 3.1 钱包与身份
- 助记词创建/导入。
- 每个用户生成用于：
  - 资金支付地址集。
  - 商品/订单消息签名身份。
  - 聊天加密密钥（建议独立于支付私钥）。
- 本地持久化（加密存储）。
- 登录后主界面需显示钱包余额与同步状态。
- 主界面提供“个人信息”按钮，进入个人资料页（昵称、头像、区域、联系方式密文）。

### 3.1.1 主界面布局（已确认）
- 买家页与卖家页分离，通过标签卡切换（Buyer/Seller）。
- 任意角色都可切换到另一侧页面（同账号双角色）。

### 3.2 商品上链与同步
- 卖家可发布商品：标题、描述、价格、库存、图片哈希/链接、发货地、标签。
- 商品更新/下架需可追溯（版本化）。
- 全网客户端可从链上同步并重建商品索引。
- 提供本地索引数据库（提升查询速度）。
- 买家页必须支持商品搜索（关键词/分类/价格区间）。
- 买家页展示“已同步卖家列表”，点击卖家直接显示该卖家商品。

### 3.2.1 商品版本与下单一致性（关键规则）
- 商品每次修改必须递增 `product_version`，最新版本为展示默认值。
- 下单时必须写入商品快照：
  - `product_id`
  - `product_version`
  - `price_snapshot`
  - `stock_snapshot`
- 卖家接单时必须校验版本一致性：
  - 一致：允许进入发货流程。
  - 不一致：订单进入“待重报价/待买家确认”，禁止按旧价直接成交。

### 3.3 订单与托管合约
- 下单时创建订单状态机。
- 支持锁定资金、发货、确认收货、退货、取消、争议终局。
- 每个状态转换都必须：
  - 有链上可验证交易或签名消息。
  - 可由第三方审计状态正确性。
- 买家与卖家都需要订单列表页，并支持按状态过滤。

### 3.4 聊天系统（端到端）
- 支持两种聊天模式：
  - 用户会话聊天（按用户维度，一人一会话）。
  - 订单会话聊天（按订单维度，订单内直接沟通）。
- 消息加密：仅双方可解密。
- 用户会话聊天（钱包间聊天）走 P2P，不上链：
  - 必须先判断对方在线，离线时不可发起会话消息。
  - 会话内容仅本地保存。
  - 消息必须做签名校验与去重（防伪造/防重放）。
- 订单会话聊天必须上链，且消息为密文（仅保留必要元数据）。
- 订单会话支持重放恢复（换设备后可从链上恢复）。

### 3.5 同步与恢复
- 主界面“同步”按钮必须弹出确认窗口后才能执行重同步：
  - 必须明确提示：会清空本地商品/分类/订单/聊天等市场数据与同步缓存。
  - 必须允许输入指定 `bootstrapHeight`，并从该高度重新下载链上数据。
  - 必须明确提示：若本地存在未上链修改，这些本地数据也可能被清空。
- 新安装客户端：
  - 从助记词恢复钱包。
  - 从链上增量同步本人相关订单/聊天。
- 网络中断后自动补同步（回放缺失区块范围）。
- 钱包身份变更（新建/恢复/切换）强制规则：
  - 触发条件：`新建钱包`、`恢复钱包`、`切换钱包` 任一操作完成后。
  - 必须清空本地钱包相关状态（防冲突）：
    - 本地商品/分类/订单/聊天投影与本地变更队列。
    - 本钱包锚点缓存与未确认缓存。
    - 运行时同步进度覆盖（避免 UI 延续旧钱包高度）。
  - 必须从指定 `bootstrapHeight` 重新同步区块：
    - 同步起点统一回到 `bootstrapHeight`，不得沿用上一个钱包的 `localHeight/networkHeight`。
    - 不允许继承上一个钱包的 `p2pHeaderCursor`、`p2pTip`、`heightHashCache`、`gapHeights`。
    - 必须隔离同步并发轮次：钱包切换后，旧轮次不得回写旧高度覆盖新钱包状态（需有会话代际/epoch 防护）。
  - 商品数据恢复策略（防“同步完成但商品为空”）：
    - 每轮同步合并链上增量时，必须同时可回填全局锚点缓存（global cache），避免仅依赖本轮扫描结果。
    - 当本地投影为空时，允许用已确认的全局缓存重建商品/分类，再由后续链上增量持续校正。
  - Windows 稳定性要求：
    - 状态文件持久化在 `rename` 失败（如 `EPERM/EACCES/EBUSY`）时必须有安全回退写入，禁止因此导致进程退出重启循环。
  - 验收标准：
    - 操作后立即可见 `localHeight = bootstrapHeight - 1`（或等价初始态）。
    - `lag` 必须按新钱包重新计算，且不与旧钱包残留高度混用。
    - 不出现跨钱包数据污染（旧钱包商品/订单/聊天不应出现在新钱包本地状态中）。

### 3.6 后台管家（本地常驻）
- 客户端内置后台管家，持续运行，不依赖中心化服务器。
- 职责：
  - 持续监听区域索引节点池，拉取最新商品上链更新。
  - 持续监听与本钱包相关订单与聊天密文消息。
  - 维护 `last_scanned_height` 并执行断线回放补扫。
  - 对发现的数据做去重、校验、入库并触发前端通知。
  - 失败重试与节点切换（单节点异常不影响整体同步）。
- 可配置项：
  - 区域优先级（本地区/邻近地区/全网兜底）。
  - 轮询频率、断线回扫区块大小、通知阈值。

## 4. 非功能需求
- 无中心化依赖：
  - 禁止“必须在线”的中心化订单服务器。
- 可验证性：
  - 任一订单状态可通过链上数据+签名重建。
- 隐私：
  - 商品公开、订单细节最小公开、聊天内容默认密文。
- 可靠性：
  - 节点断连后自动重连与回放。
  - 后台管家崩溃可自动拉起，保证同步连续性。

## 5. 关键设计（已确认 + 待确认）
1. **争议资金去向（已确认）**
   - 罚没/争议资金进入买卖双方共管地址。
   - 仅在双方同意后，或交易取消后，按规则退回各自钱包。
2. **聊天存储（已确认）**
   - 聊天可上链，但内容必须是密文。
3. **商品图片（已确认）**
   - 图片允许上链。
4. **托管实现方式（待确认）**
   - 采用方案A：多签托管脚本（按双方共管地址模型实现）
   - 共管地址默认 `2-of-2`（买家 + 卖家）
5. **链上数据协议（待细化）**
   - 商品、订单、聊天消息的统一数据格式（版本号必须有）。

## 6. 建议的阶段划分
- Phase 1（MVP）：
  - 钱包、商品发布/浏览、下单、锁定/确认/取消、基础聊天、后台管家基础同步。
- Phase 2：
  - 退货流程、争议终局、后台管家高级重同步、更完整检索。
- Phase 3：
  - 信誉系统、搜索排序、移动端适配。

## 7. 当前建议的“最小可跑版本”
- 已确认：MVP 直接使用 **方案A（双签/多签托管）**，避免后续资金逻辑返工。
- 这样第一版就不会在核心资金规则上返工。

---

## 8. 新增需求（2026-03-01）
1. **钱包级店铺活跃指标**
   - 每个钱包/店铺维护活跃度字段（`activityScore`、`lastActiveAt`、`active`）。
2. **活跃判定阈值**
   - 活跃判定窗口固定为 `180天`。
   - `lastActiveAt` 在最近 180 天内则视为活跃，否则视为不活跃。
3. **活跃事件来源**
   - 以下任一事件更新时间即刷新 `lastActiveAt`：
     - 交易事件（订单/支付相关上链记录）
     - 商品上链事件（发布/修改/下架）
     - 聊天事件（订单会话或用户会话）
4. **全网店铺过滤**
   - 网络侧默认过滤不活跃店铺（`active=false` 不进入默认展示列表）。
   - 买家页可提供“显示不活跃店铺”开关（默认关闭）。
5. **跨区域活跃（预留）**
   - 活跃判定可扩展区域维度（本地活跃/跨省活跃），当前先按统一 180 天规则落地。

---

## 已确认实现基线
1. MVP 先开发可用闭环版本（先可跑再迭代）。
2. 共管地址使用 `2-of-2`（买卖双方共同签名）。
3. 聊天策略拆分：
   - 钱包间聊天走 P2P（不上链，需在线检测，本地保存，签名校验+去重）。
   - 订单聊天上链且必须是密文。
4. 商品图片允许上链。

---

## 9. 新增需求（2026-03-02）
1. **钱包间聊天改为 P2P（不上链）**
   - 钱包与钱包之间的普通会话聊天，不写链。
   - 发送前必须判断对方在线；不在线不可聊。
   - 会话内容仅本地保存。
   - 所有消息必须经过签名校验和去重处理。
   - 连接策略采用“直连优先 + P2P 节点中继兜底”：
     - 先尝试 UDP 直连/打洞。
     - 直连失败时自动切换到去中心化中继路径（不依赖中心化聊天服务器）。
2. **订单聊天保留上链**
   - 与订单关联的会话必须上链，作为订单证据链的一部分。
   - 上链消息必须为密文。
3. **P2P 聊天端口（首版固定）**
   - 首版仅使用单端口：`UDP 8334`。
   - 同一端口承载在线探测、打洞握手、聊天消息、ACK 去重确认。
   - 端口可配置，默认值 `8334`。
4. **P2P 聊天发现协议 v1（去中心化）**
   - 采用类 BSV 节点发现机制（`addr/getaddr/gossip` 思路），但使用独立聊天协议字段。
   - 每个节点必须发布并签名自己的聊天可达信息：
     - `nodeId(pubkey)`
     - `host/ip`
     - `chatPort`
     - `timestamp`
     - `signature`
   - 任何节点可向邻居请求地址表（`getaddr`），并按 gossip 扩散最新地址信息。
   - 本地维护联系人表与健康评分（成功/失败/最近在线），不依赖中心化目录。
   - 节点能力公告可声明 `stun`、`signaling` 等能力位，默认不得声明数据转发型 `relay` 能力。
   - 端口变更后必须立即重新发布签名公告，其他节点按时间戳更新联系人表。
   - 所有发现消息必须签名校验，防止地址伪造与端口劫持。
5. **公网节点自动升级为信令与 STUN 候选（强制）**
   - 软件启动后必须判断本机当前对外 IP 是否为全局公网 IP。
   - 判断方式必须同时排除本地、内网、链路本地、保留地址、运营商级 NAT、组播、文档示例地址等非全局地址段。
   - 如果本机拥有全局公网 IP，且用户没有显式关闭该能力，服务必须自动启动：
     - P2P signaling / rendezvous 服务；
     - STUN 绑定探测服务；
     - 节点能力公告与健康自检任务。
   - 公网节点默认不得启动 TURN/relay 数据转发服务，避免占用公网节点流量；只有显式运维策略允许时才可启用。
   - 公网节点必须把自身能力签名公告到发现层，至少包含：
     - `nodeId`
     - `publicIp`
     - `signalEndpoint`
     - `stunEndpoint`
     - `capabilities: { signaling, rendezvous, stun, turn: false, relay: false }`
     - `timestamp`
     - `signature`
   - 非公网节点不得误公告自己为公网中继，但可以作为普通客户端使用已发现的公网 signaling/STUN 节点。
   - 客户端发起 P2P 连接时必须优先使用已验证的公网节点列表作为 ICE/STUN 候选；候选失败时自动切换下一个候选或回退链上密文消息。

---

## 10. P2P 安全基线 v1（2026-03-02）
1. **密钥隔离（强制）**
   - 聊天签名密钥必须与支付私钥隔离，不允许直接复用资金签名私钥。
   - 聊天会话密钥可轮换，且本地加密保存。
2. **消息结构（强制）**
   - P2P 聊天消息统一结构至少包含：
     - `msgId`（全局唯一）
     - `sessionId`
     - `nonce`
     - `ts`（时间戳）
     - `fromNodeId`
     - `signature`
   - 缺失关键字段或签名校验失败的消息必须丢弃。
3. **防重放与去重（强制）**
   - 必须实现 anti-replay 窗口（按 `ts/nonce/msgId` 联合校验）。
   - 本地维护去重缓存与持久化索引，防止重启后重复投递。
   - 超出时间窗的历史消息默认拒收。
4. **发现层防护（强制）**
   - `addr/getaddr/gossip` 消息均需签名校验。
   - 必须有限流、失败惩罚、评分衰减、黑名单与过期机制。
   - 防止地址投毒、Sybil、Eclipse 等发现层攻击导致大面积不可达。
5. **在线状态三态模型（强制）**
   - 必须区分并展示：
     - `在线`（节点有心跳）
     - `可达`（握手可达）
     - `可聊天`（端到端探测通过）
   - 仅 `可聊天` 状态允许发送普通 P2P 聊天消息。
6. **订单聊天强绑定（强制）**
   - 订单聊天消息必须绑定 `orderId`，并按订单证据链上链。
   - 普通 P2P 聊天与订单聊天必须严格隔离，禁止混用证据通道。
7. **公网 STUN 可用性基线（强制）**
   - 当直连需要 NAT 探测时，客户端必须自动尝试已验证的公网 STUN 节点。
   - 公网 STUN 节点只回复地址探测，不转发聊天数据。
   - STUN 失败时必须自动切换备选候选，并给出“在线/可达/可聊天失败原因”提示。
   - 如果网络中存在已验证公网节点，该节点应自动成为 STUN 候选；例如 `8.136.3.174` 这类公网节点应在健康检查通过后进入候选池。
   - STUN 候选必须有健康评分、失败惩罚、过期时间和重新验证机制，避免长期使用不可达或过期公网节点。
8. **链上聊天与点对点聊天并存（强制）**
   - 系统必须长期同时支持链上聊天与点对点聊天，两种传输并存，不允许把其中一种视为过渡态后移除。
   - 普通用户会话可优先走 P2P，订单会话必须支持链上密文消息。
   - 统一聊天窗口中必须能同时展示两类消息，但每条消息都保留明确的传输标记。
9. **聊天模块完全独立（强制）**
   - 聊天模块必须作为独立域实现，拥有自己的运行时状态、投影、存储、后台任务与故障恢复逻辑。
   - 聊天模块与钱包、订单、商品、同步等其它模块之间，只能通过事件总线/运行时总线通讯。
   - 禁止聊天模块直接写入其它模块内部状态，也禁止其它模块直接写入聊天内部状态。
10. **上线/离线状态广播与 P2P 监听切换（强制）**
   - 用户必须能够广播自己的上线/离线状态变更，并让其它用户感知该状态变化。
   - 当用户切换为上线时，系统应启动 P2P 聊天监听、握手、可达性检测与普通会话收发能力。
   - 当用户切换为离线时，系统必须自动关闭 P2P 监听与相关后台探测，不再接受新的普通 P2P 会话消息。
   - 离线状态不影响链上聊天的同步、读取与订单证据链处理；重新上线后，P2P 监听与状态广播应可恢复。

---

## 11. 同步提速策略（2026-03-03）
1. **多节点并行分片同步（管家）**
   - 一轮同步可同时向多个可用数据节点请求分片数据（不限于单源）。
   - 同步任务按高度区间拆分为小分片，支持并行拉取后汇总。
2. **动态分片调度**
   - 管家按节点实时表现动态分配分片权重：
     - 成功率高、延迟低、吞吐高的节点分配更多分片。
     - 失败率高或超时节点降低权重并触发短时熔断。
3. **统一合并与幂等**
   - 分片结果统一由管家合并，按 `height + txid + eventType` 去重。
   - 所有实体继续遵循版本管理、迁移链、防回退规则，保证一致性。
4. **轮询频率优化**
   - 管家默认轮询周期从 `20s` 调整为 `15s`，缩短轮次间空档。
## Runtime Log Layout

- Linux and Windows runtime logs must live under the project-local `log/` directory, not `%TEMP%` or `/tmp` for primary app logs.
- Primary runtime logs must use stable relative paths so cross-platform debugging is consistent:
  - `log/bsv_market_run.log`
  - `log/bsv_market_node2.log`
  - `log/market-debug.log`
  - `log/steward-worker.log`
  - `log/recover-debug.log`
  - `log/send-debug.log`
- The `data/` directory remains for wallet/state/cache/spool data, not for long-running application log files.
- Windows helper scripts and debug collection scripts must read from `log/` first.
