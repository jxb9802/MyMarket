# BSV Market 仅用 SPV 的可行性分析

## 结论（先说结果）
- **可以做**：钱包签名、广播交易、验证与自己相关交易的 Merkle 证明、订单资金锁定/释放。
- **不能天然做到**：仅靠传统 SPV 自动发现“全网所有商品数据”。
- **工程上可落地方案**：
  - 资金层用双签/多签共管地址（2-of-2）。
  - 数据层定义统一链上协议（商品/订单/聊天消息前缀 + 版本）。
  - 发现层引入“去中心化索引节点池”（任何人都可跑，不是单点服务器）。

## 当前项目新增约束
- 系统同步本身从固定高度开始，不需要从创世块扫起。
- 系统内钱包地址只会在该固定高度之后开始使用。
- 当前钱包地址窗口可固定为前 20 个地址。
- 发送规则仍要求 strict BEEF，所以本地必须维护待花链的上下文完整性。
- WOC 不应继续停留在常规发送关键路径，最多只保留为 fallback / 修复工具。
- 日常只保留统一的区块同步，不再保留独立钱包同步。
- 外部导入钱包允许用 WOC 做一次初始化建档，完成后回到本地增量索引体系。

## 对当前项目的直接结论
- 在以上约束成立时，钱包层不必继续依赖 WOC 地址扫描来重建 UTXO。
- 更合适的方向是：
  - 将“钱包相关交易发现”并入现有固定高度区块同步。
  - 用后台管家执行 P2P 扫块 + 本地索引，维护 UTXO、花费关系和上下文。
  - 主进程只负责读状态和下达命令，不再亲自执行重型同步任务。
  - 发送前检查只依赖本地钱包索引，而不是再次触发全量钱包重建。
  - WOC 仅用于导入钱包初始化和手工修复。

## 具体设计
- 详见 [docs/WALLET_P2P_INDEX_V1.md](./docs/WALLET_P2P_INDEX_V1.md)

## 1. 只用 SPV 能做什么
1. 下载区块头并验证 PoW 链。
2. 验证与你钱包地址相关交易是否进块（Merkle Proof）。
3. 广播订单相关交易（锁定、释放、退款）。
4. 本地维护“与本钱包相关”的 UTXO 集和订单状态。

## 2. 为什么“商家发现”是难点
- SPV默认是“按需验证我关心的交易”，不是“扫描全链所有应用数据”。
- 你要做市场，必须能发现全网商品上链消息：
  - 这本质上需要**全链应用层索引**能力。
- 因此若完全没有任何索引协助，用户会遇到：
  - 首次同步极慢或无法完整发现商品。
  - 商品检索体验差（按关键词、标签、店铺）。

## 3. 可行架构（保持去中心化）
- 不是“一个中心服”，而是 **索引节点池**：
  - 协议公开，任何人可运行索引节点。
  - 客户端内置多个默认索引节点 + 可手动添加。
  - 客户端对索引结果做随机抽样校验（抽查 txid + merkle proof）。
- 这样仍符合“无中心化依赖”：
  - 任何节点可离线，客户端可切换。
  - 协议和数据都在链上，索引只是加速层。

### 3.1 后台管家如何配合索引节点池
- 每个钱包本地运行“后台管家（Background Steward）”：
  - 常驻轮询本地区索引池，订阅最新商品和聊天更新。
  - 周期性回放最近窗口区块，避免实时监听漏事件。
  - 多节点比对后入本地索引库，触发 UI 更新。
- 这样可以做到：
  - 不依赖单个中心服务。
  - 即使某次监听丢失，也可通过回放补回数据。

## 4. 商品信息下载建议
- 商品发布交易写入标准化 payload：
  - `app=bsv_market`
  - `type=product_upsert|product_offline`
  - `version=1`
  - `shop_pubkey`
  - `product_id`
  - `title/price/stock/image_refs/hash`
- 图片可上链：
  - 可行，但会增加成本和同步压力。
  - 建议支持两种模式：
    1) 小图直接上链（缩略图）
    2) 原图分片上链或外链 + 哈希校验

## 5. 保证金/共管地址处理（你的规则）
- 推荐：订单资金脚本采用 **2-of-2 买卖双方多签 UTXO**。
- 资金流：
  1. 买家下单：锁定 `商品价 + 20%` 到共管UTXO。
  2. 卖家发货：再锁定 `10%` 到同一订单资金池（或关联UTXO）。
  3. 确认收货：双方签名释放，按规则分账。
  4. 退货取消：双方签名退款。
  5. 争议：资金继续锁定在共管地址，直到双方后续协商签名解锁。
- 优点：
  - 规则与你定义完全一致。
  - 无第三方即可执行（但争议会卡住直到双方协商）。

## 6. 聊天密文上链
- 可实现：
  - 每条消息使用会话密钥加密。
  - 链上保存密文 + 最小元信息（order_id, seq, sender_pubkey, nonce）。
- 要点：
  - 消息加密密钥要和支付私钥隔离。
  - 支持换设备恢复（从助记词派生会话解密能力，或额外密钥备份）。

## 7. MVP 技术边界建议
- MVP可以“仅SPV+去中心化索引池”落地。
- MVP不建议做“纯SPV零索引”全网商品发现，因为体验会不可用。
- 你的需求中最关键的是资金规则与订单状态机，建议优先保证这两块严谨可审计。

## 8. 开发阶段建议（按你当前目标）
1. 先做演示页/状态机（已完成）。
2. 定义链上数据协议 v1（product/order/chat）。
3. 实现2-of-2共管资金交易构建与签名流程。
4. 实现后台管家 + 最小索引层（可多节点、可替换、可校验）。
5. 再做界面联调和真实链广播。
