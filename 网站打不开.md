# 网站打不开

适用症状：
- 浏览器显示 `ERR_CONNECTION_REFUSED`
- `curl http://127.0.0.1:8091/api/state` 连接被拒绝

固定处理流程（按顺序执行）：

1. 进入项目目录

```bash
cd /home/jb9802/.openclaw/workspace/app/bsv_market
```

2. 用控制脚本重启并检查状态

```bash
./market_ctl.sh restart
./market_ctl.sh status
./market_ctl.sh probe
```

3. 若 `probe` 不是 `HTTP/1.1 200 OK`，执行一键诊断（含重启）

```bash
./diagnose_market.sh --restart
```

4. 检查最新诊断报告（看 200/报错）

```bash
ls -t data/diagnostics/diagnose-*.log | head -n 1
```

5. 查看运行日志尾部（定位崩溃/端口问题）

```bash
tail -n 120 /tmp/bsv_market_run.log
```

6. 验证网页

- 打开：`http://127.0.0.1:8091/index.html`
- 或命令行验证：

```bash
curl -I http://127.0.0.1:8091/index.html
curl -I http://127.0.0.1:8091/app.js
curl -I http://127.0.0.1:8091/api/state
```

## 一键修复（推荐）

已固化为单脚本，优先使用：

```bash
cd /home/jb9802/.openclaw/workspace/app/bsv_market && ./scripts/fix_site_unavailable.sh
```

该脚本会自动执行：
- 重启服务（失败时自动走诊断重启）
- 增加健康等待窗口（避免“刚启动未就绪就被判失败”）
- 检查健康状态（`running+healthy`）
- 探测 `/api/state`（应返回 `HTTP/1.1 200 OK`）

已知有效标准：
- `./market_ctl.sh status` 显示 `running+healthy`
- `./market_ctl.sh probe` 返回 `HTTP/1.1 200 OK`
- 三个 URL 至少返回 `200 OK`

补充说明：
- 若看到 `running+unhealthy`，表示进程在但接口不通，需要立刻执行 `./market_ctl.sh restart` 或 `./diagnose_market.sh --restart`。

本次实测（2026-03-01）：
- `./scripts/fix_site_unavailable.sh` 已验证可恢复服务并返回 `HTTP/1.1 200 OK`。

常见误操作：
- 不要把命令拆成两行执行（例如先输入 `./` 再输入脚本名），否则会出现 `bash: ./: 是一个目录`。

备注：
- 本项目已将静态资源改为白名单 `fs.readFile` 返回，避免 `sendFile Not Found` 的环境兼容问题。
- 后续再次出现“网站打不开”，默认直接执行本文件流程，不再从零排查。
- 用户侧统一执行单条命令：`cd /home/jb9802/.openclaw/workspace/app/bsv_market && ./scripts/fix_site_unavailable.sh`。
